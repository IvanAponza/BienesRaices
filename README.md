# BienesRaices


## Instalación 

1. Clonar .env.template a .env y configurar las variables de entorno `cp .env.template .env`
2. Ejecutar `npm install` para instalar las dependencias
3. Ejecutar `npm run server` para levantar el proyecto en modo desarrollo.
4. Ejecutar `npm run dev` para cargar los archivos css y js.

## Ejecutar en modo desarrollo
4. Rellenar la base de datos y con datos de prueba `npm run db:importar`, crea usuarios, pruductos y categorias.
4. Eliminar datos de la base de datos `npm run db:eliminar`
